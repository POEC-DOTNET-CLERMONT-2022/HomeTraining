<Page x:Class="WPFClient.AllExerciceView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WPFClient"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="AllExerciceView">

    <Grid Background="White">
        
        <ListBox Grid.ColumnSpan="1" SelectedItem="{Binding Currentexercice}" ItemsSource="{Binding Exercices}" x:Name="WindowListUser" SelectedIndex="0" Margin="0,65,0,0" >
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <local:ExerciceElementControl Exercice="" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Button Content="Button" HorizontalAlignment="Left" Margin="62,36,0,0" VerticalAlignment="Top" Click="Button_Click"/>
    </Grid>
</Page>
